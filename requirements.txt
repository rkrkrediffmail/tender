# Core Flask framework
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.1
Flask-Login>=0.6.3
Flask-WTF>=1.2.1
Flask-Migrate>=4.0.7
Flask-CORS>=4.0.0

WTForms==3.1.1
Jinja2==3.1.2

# WSGI server for production
gunicorn==21.2.0

# Database drivers (Latest)
psycopg2-binary==2.9.9
SQLAlchemy==2.0.25

# Document processing (Essential only)
PyPDF2==3.0.1
python-docx==1.1.0
openpyxl==3.1.2
Pillow==10.2.0  # Image processing - needed for document processor
# pytesseract==0.3.10  # Heavy OCR - commented out for minimal build
# python-magic==0.4.27  # Not essential
# weasyprint>=59.0  # Heavy PDF generation - commented out
reportlab==4.0.7  # Lightweight PDF generation for analysis reports

# Security and utilities (Latest)
Werkzeug==3.0.1
bcrypt==4.1.2
cryptography==42.0.2
PyJWT==2.8.0
requests==2.31.0
beautifulsoup4==4.12.2
python-dateutil==2.8.2
python-dotenv==1.0.0

# AI APIs
anthropic==0.57.1
openai==1.98.0

# LangChain and Vector Database (LIGHTWEIGHT)
# Heavy packages commented out to reduce image size from 8GB to ~1GB
# sentence-transformers==2.3.1  # This alone is ~2GB
# faiss-cpu==1.8.0  # Another ~500MB
# langchain==0.1.20  # Full package is heavy
# langchain-community==0.0.38  # Heavy dependencies

# Minimal vector support (only if DATABASE_URL includes pgvector)
# Commented out for minimal build - uncomment if vector features needed
# langchain-core==0.1.52
# langchain-openai==0.1.8
# pgvector>=0.2.5,<0.3.0

# Lightweight fallback for similarity search
scikit-learn==1.3.0
numpy==1.24.3

#python-magic-bin
# mammoth==1.5.1  # For advanced DOCX processing - commented for minimal build
# textract==1.6.5  # Alternative text extraction - heavy package
#pdfplumber==0.9.0  # Better PDF text extraction
# pdfplumber  # Commented for minimal build

# File handling
filetype==1.2.0

# Azure integration (Enhanced for ephemeral storage)
azure-identity==1.15.0
azure-storage-blob==12.19.0
azure-keyvault-secrets==4.7.0
azure-core>=1.29.0

# Caching (lightweight - no Redis needed)
flask-caching==2.1.0

# Monitoring and health checks
#flask-healthz==0.0.3

flask-healthz
flask-limiter==3.5.0
reportlab
# Input validation
marshmallow==3.20.1

# Development and testing
pytest==7.4.3
pytest-flask==1.3.0
